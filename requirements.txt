# ── Core dependencies (always installed with pip install cofone) ───────────────
openai>=1.0.0           # LLM + embedding API calls (OpenAI-compatible interface)
python-dotenv>=1.0.0    # .env file support
httpx>=0.27.0           # HTTP client for web sources and Jina embeddings
beautifulsoup4>=4.12.0  # HTML parsing for web URL loading
pydantic>=2.0.0         # structured output validation

# ── Optional: PDF support (pip install "cofone[pdf]") ─────────────────────────
pypdf>=4.0.0

# ── Optional: FAISS semantic search (pip install "cofone[faiss]") ─────────────
faiss-cpu>=1.7.0
sentence-transformers>=2.0.0

# ── Optional: Web sources (pip install "cofone[web]") ─────────────────────────
wikipedia>=1.4.0
youtube-transcript-api>=0.6.0

# ── Optional: API embedding providers (install manually as needed) ─────────────
cohere>=5.0.0
voyageai>=0.2.0